# TeeOne credentials
TEEONE_USERNAME=48273548K
TEEONE_PASSWORD=48273548K

# Google Sheets - Path to service account JSON (create in Google Cloud Console)
GOOGLE_CREDENTIALS_PATH=./credentials/google-service-account.json

# Google Sheet ID (from URL: docs.google.com/spreadsheets/d/SHEET_ID/edit)
GOOGLE_SHEET_ID=1QyNz-qwAVfmoxSazewn3nwQLjNTR7zx0TpAM3qtWflA

# Sheet range (tab name + columns). Use "Hoja 1" if your sheet is in Spanish
# GOOGLE_SHEET_RANGE=Sheet1!A:Z

# Optional: set to false to see the browser (for debugging)
# HEADLESS=true

# Optional: wait in ms after selecting Jugadores, before Hora de juego (default 1000). Increase for debugging.
# DEBUG_WAIT_AFTER_JUGADORES_MS=5000

# Email notification (Gmail example - use App Password if 2FA enabled)
EMAIL_FROM=nicogonzalez@gmail.com
EMAIL_PASSWORD=plsd qspz wadc kwiw
EMAIL_TO=nicogonzalez@gmail.com
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587

# Optional email diagnostics/tuning
# EMAIL_SMTP_STARTTLS=true
# EMAIL_SMTP_SSL=false
# EMAIL_TIMEOUT_MS=15000
# EMAIL_DEBUG=true
